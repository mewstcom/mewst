# コードレビュー: dep-4-1a

## レビュー情報

| 項目                       | 内容                                                |
| -------------------------- | --------------------------------------------------- |
| レビュー日                 | 2026-03-31                                          |
| 対象ブランチ               | dep-4-1a                                            |
| ベースブランチ             | dep-4-1                                             |
| 作業計画書（指定があれば） | docs/plans/1_doing/depguard-alignment.md            |
| 変更ファイル数             | 7 ファイル（うちドキュメント・レビュー 2 ファイル） |
| 変更行数（実装）           | +91 / -63 行                                        |
| 変更行数（テスト）         | +181 / -76 行                                       |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go 版の開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド

## 変更ファイル一覧

### 実装ファイル

- [x] `go/internal/email/confirmation.go`
- [x] `go/internal/usecase/send_email_confirmation.go`

### テストファイル

- [x] `go/internal/email/confirmation_test.go`
- [x] `go/internal/usecase/send_email_confirmation_test.go`

### 設定・その他

- [x] `go/.golangci.yml`
- [x] `docs/plans/1_doing/depguard-alignment.md`
- [x] `docs/reviews/dep-4-1a-001.md`

## ファイルごとのレビュー結果

問題のあるファイルはありません。すべてのファイルがガイドラインに準拠しています。

## 設計との整合性チェック

### 作業計画書タスク 4-1a との比較

**作業計画書の要件**:

1. `internal/email/confirmation.go` に `ConfirmationSender` を新設 → ✅ 実装済み
2. `internal/usecase/send_email_confirmation.go` から `internal/templates` import を削除し、`EmailConfirmationSender` interface パターンに変更 → ✅ 実装済み
3. application-layer に `templates` deny を再追加 → ✅ 実装済み
4. email-layer の depguard ルールを新設 → ✅ 実装済み
5. `main.go` の DI 構成を更新（`ConfirmationSender` を構築して UseCase に注入） → ⚠️ 未実施（下記参照）
6. テスト更新 → ✅ 実装済み
7. `make lint` で違反がないことを確認 → 確認が必要

### `main.go` の DI 構成について

**ステータス**: 要確認

作業計画書のタスク 4-1a に「`main.go` の DI 構成を更新（`ConfirmationSender` を構築して UseCase に注入）」と記載されていますが、`cmd/server/main.go` は変更ファイルに含まれていません。

現状を調査すると、`SendEmailConfirmationUsecase` は `main.go` で DI されておらず、Worker（`send_email_confirmation.go`）がまだ直接テンプレートレンダリングを行っています。Worker が UseCase を呼び出すようになるのはタスク 4-2 の責務であるため、**現時点で `main.go` の DI 更新が不要であることは技術的に正しい**です。

ただし、作業計画書の記載と実装の乖離があるため、以下のいずれかの対応が望ましいです。

**対応方針**:

<!-- 開発者が回答を記入してください -->

- [ ] 作業計画書のタスク 4-1a から「`main.go` の DI 構成を更新」の記載を削除し、タスク 4-2 に移動する
- [x] 現時点で `main.go` の DI も更新する（Worker はまだ UseCase を使わないが、DI 構成だけ先に追加）
- [ ] その他（下の回答欄に記入）

**回答**:

```
（ここに回答を記入）
```

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Approve

**総評**:

作業計画書のタスク 4-1a の核心的な要件（テンプレートレンダリングの email パッケージへの移動、UseCase の interface 化、depguard ルール追加）はすべて正しく実装されています。

**良かった点**:

- `ConfirmationSender` の設計が作業計画書の設計通りで、email パッケージにテンプレートレンダリングと i18n 件名取得を適切に閉じ込めている
- UseCase 側で `EmailConfirmationSender` interface を定義し、`internal/templates` への依存を完全に排除できている
- email-layer の depguard ルールが作業計画書の設計と一致している
- テストが適切に更新されている（email パッケージ側はテンプレートレンダリングの統合テスト、UseCase 側はモックを使った単体テスト）
- コメントが日本語で記述されており、ガイドラインに準拠している

`main.go` の DI 構成に関する作業計画書との乖離は軽微であり、タスク 4-2 で解消される見込みのため、Approve とします。
