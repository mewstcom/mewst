ja:
  activerecord:
    attributes:
      profile:
        atname: At name
      verification:
        code: Verification code
        email: Email
    errors:
      models:
        twitter_account:
          attributes:
            base:
              permitted_scopes: "Unpermitted scopes exist: %{unpermitted_scopes}"
