<%= form_with model: @command, url: sign_in_path do |f| %>
  <%= f.label :phone_number_origin %>
  <%= render Inputs::PhoneNumberInputComponent.new(form: f, field_name: :phone_number_origin, autofocus: true) %>

  <%= f.submit %>
<% end %>
