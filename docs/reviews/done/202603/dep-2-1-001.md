# コードレビュー: dep-2-1

## レビュー情報

| 項目                       | 内容                                     |
| -------------------------- | ---------------------------------------- |
| レビュー日                 | 2026-03-30                               |
| 対象ブランチ               | dep-2-1                                  |
| ベースブランチ             | dep-1-1                                  |
| 作業計画書（指定があれば） | docs/plans/1_doing/depguard-alignment.md |
| 変更ファイル数             | 6 ファイル（実装対象）                   |
| 変更行数（実装）           | +35 / -16 行                             |
| 変更行数（テスト）         | +29 / -24 行                             |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go版の開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド
- [@go/docs/security-guide.md](/workspace/go/docs/security-guide.md) - セキュリティガイドライン

## 変更ファイル一覧

### 実装ファイル

- [x] `go/internal/auth/token.go`
- [x] `go/internal/session/manager.go`
- [x] `go/internal/usecase/create_session.go`

### テストファイル

- [x] `go/internal/auth/token_test.go`
- [x] `go/internal/session/manager_test.go`

### 設定・その他

- [x] `go/.golangci.yml`
- [x] `docs/plans/1_doing/depguard-alignment.md`（作業計画書の更新）
- [x] `docs/reviews/done/202603/dep-1-1-001.md`（前回レビューのフォーマット修正）

## 設計との整合性チェック

作業計画書のタスク 2-1 の要件:

| 要件                                                                    | 実装状況    |
| ----------------------------------------------------------------------- | ----------- |
| `internal/auth/token.go` に `GenerateSecureToken()` 関数を追加          | ✅ 実装済み |
| Wikino と統一した関数名 (`GenerateSecureToken`)                         | ✅ 実装済み |
| `internal/usecase/create_session.go` の import を `session` → `auth` に | ✅ 実装済み |
| `internal/session/` から `GenerateToken()` を削除                       | ✅ 実装済み |
| application-layer に `session` deny を追加                              | ✅ 実装済み |
| テストの移動                                                            | ✅ 実装済み |

### 残参照の確認

`session.GenerateToken` の参照がコードベースに残っていないことを確認済み。

## ファイルごとのレビュー結果

問題のあるファイルはありません。全ファイルがガイドラインに従っています。

### レビュー詳細

**`go/internal/auth/token.go`**:

- `crypto/rand` を使用した安全なトークン生成 ✅
- コメントは日本語で記述 ✅
- Rails の `has_secure_token` との互換性が明記されている ✅
- 24バイト → Base64 URL-safe = 32文字の仕様が元の `session.GenerateToken` と同一 ✅

**`go/internal/auth/token_test.go`**:

- `t.Parallel()` 使用 ✅
- トークン長の検証 ✅
- ユニーク性の検証（2回生成して異なることを確認） ✅
- テストはDB不要のため `TestMain` パターン不要で問題なし ✅

**`go/internal/session/manager.go`**:

- `GenerateToken()` と `crypto/rand`, `encoding/base64` の import が正しく削除されている ✅
- Manager の他の機能に影響なし ✅

**`go/internal/session/manager_test.go`**:

- `TestGenerateToken` テストが正しく削除されている ✅
- 他のテスト（`TestManager_GetSessionToken` 等）に影響なし ✅

**`go/internal/usecase/create_session.go`**:

- `session` import が `auth` import に正しく置換されている ✅
- `session.GenerateToken()` → `auth.GenerateSecureToken()` に変更 ✅
- アーキテクチャルール準拠: UseCase は `auth`（Domain/Infrastructure層）に依存しており、`session`（Presentation層のヘルパー）への依存を解消 ✅

**`go/.golangci.yml`**:

- application-layer に `session` deny が正しく追加されている ✅
- desc メッセージが Wikino と整合する内容（「トークン生成はauthパッケージを使用してください」） ✅
- 既存のルールに影響なし ✅

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Approve

**総評**:

作業計画書タスク 2-1 の要件がすべて正しく実装されている。`session.GenerateToken()` を `auth.GenerateSecureToken()` に移動するリファクタリングは、以下の点で適切に実施されている：

1. **アーキテクチャの改善**: UseCase（Application層）から session（Presentation層のヘルパー）への依存を解消し、auth（Domain/Infrastructure層）への依存に変更。依存の方向が正しい
2. **Wikino との整合性**: 関数名 `GenerateSecureToken` が Wikino と統一されている
3. **テストの移動**: テストコードも正しく `auth` パッケージに移動され、テスト内容は元と同一
4. **depguard ルール**: application-layer に `session` deny が追加され、今後の違反を静的解析で防止可能
5. **後方互換性**: `session.GenerateToken` の参照がコードベースに残っていないことを確認済み

変更行数が小さく（実装 +35/-16、テスト +29/-24）、リファクタリングとして適切な粒度のPRである。
