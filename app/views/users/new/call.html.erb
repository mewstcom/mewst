<%= form_with model: @user_creator, url: user_list_path do |f| %>
  <%= render Cards::FormErrorsCardComponent.new(errors: f.object.errors) %>

  <div>
    <%= f.label :phone_number %>
    <%= @phone_number_verification.phone_number %>
  </div>

  <div>
    <%= f.label :idname %>
    <%= f.text_field :idname, autofocus: true %>
  </div>

  <%= f.submit %>
<% end %>
