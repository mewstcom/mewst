<%= render Navbars::NavbarComponent.new %>

<div class="container">
  <%= render Forms::PostFormComponent.new(post_creator: @post_creator) %>

  <div>
    <% @posts.each do |post| %>
      <div>
        <div>
          @<%= post.profile.idname %>
        </div>
        <%= post.content %>
      </div>
    <% end %>
  </div>
</div>
