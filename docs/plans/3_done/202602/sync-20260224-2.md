# プロジェクト間ファイル同期 2026/02/24

## 仕様書

- なし（同期タスクのため仕様書は不要）

## 概要

Mewst・Annict・Wikino の 3 プロジェクト間でガイドライン・設定ファイルの差分を調査し、同期を行う。

## 設計

### 差分 1: `CLAUDE.md` - ハンドラー標準ファイル名の数

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Mewst**: 「9 種類のみ」と記載
- **Annict**: 「8 種類のみ」と記載
- **Wikino**: 「8 種類のみ」と記載

**採用方針**:

- [x] Mewst の方式を採用（他プロジェクトに反映）
- [ ] Annict/Wikino の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: 実際のハンドラーガイド（`go/docs/handler-guide.md`）では 3 プロジェクトとも同じ 9 種類（handler.go, index.go, show.go, new.go, create.go, edit.go, update.go, delete.go, validator.go）を定義しています。ルート `CLAUDE.md` の記載が Annict/Wikino で「8」になっているのは記載ミスの可能性があります。実態に合わせて「9」に統一するのを推奨します。

---

### 差分 2: `CLAUDE.md` - Dev Container の記載

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Mewst**: 「Dev Container を使用した開発環境」と記載
- **Annict**: 「Dev Container」と記載
- **Wikino**: Dev Container への言及なし（「Docker 及び Docker Compose がインストール済み」のみ）

**採用方針**:

- [ ] Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [x] その他: Dev Containerは現在使っていないと思うので、使われていないことを確認した上で記載を削除してください。

> **補足**: 3 プロジェクトとも Dev Container を使用しているため、明記するのが正確です。

---

### 差分 3: `go/CLAUDE.md` - テストヘルパー `SetupTestDB(t)` のリスト

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Mewst**: テストヘルパー一覧に `SetupTestDB(t)` を含む
- **Annict**: `SetupTestDB(t)` を含まない（`SetupTestMain`, `SetupTx`, `GetTestDB` のみ）
- **Wikino**: `SetupTestDB(t)` を含まない（同上）

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: `SetupTestDB` は `TestMain` パターン導入前の古いヘルパーで、現在は `SetupTestMain` + `SetupTx` に置き換わっています。リストから削除するのが正確です。

---

### 差分 4: `go/CLAUDE.md` - Go 版で処理するパスの例の詳細度

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Mewst**: `/sign_in` など（簡略）
- **Annict**: `/static/*`, `/health`, `/manifest.json`, `/sign_in`, `/password/*` など（詳細に列挙）
- **Wikino**: `/sign_in` など（簡略）

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] その他: <!-- 記入 -->

> **補足**: プロキシ対象パスはプロジェクトごとに異なるため、実態に合わせて各プロジェクトで管理するのが適切です。

---

### 差分 5: `go/docs/architecture-guide.md` - Presentation 層ヘルパーの `image` パッケージ

**対象ファイル**: `go/docs/architecture-guide.md`

**差分の内容**:

- **Mewst**: Presentation 層ヘルパーに `i18n, session` のみ記載
- **Annict**: `image, i18n, session`（`image` パッケージが追加）
- **Wikino**: `image, i18n, session`（`image` パッケージが追加）

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] その他: <!-- 記入 -->

> **補足**: Mewst には現時点で `internal/image` パッケージが存在しない可能性があるため、プロジェクト固有の差分です。

---

### 差分 6: `go/.golangci.yml` - templates 層の files パターン

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Mewst**: `"**/internal/templates/**/*.go"` （`.go` サフィックス付き）
- **Annict**: `"**/internal/templates/**"` （サフィックスなし）
- **Wikino**: `"**/internal/templates/**"` （サフィックスなし）

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: `.go` サフィックスを付けると `.templ` ファイルが除外されますが、golangci-lint は Go ファイルのみ解析するため実質的な影響はありません。ただし、他プロジェクトと統一する方がシンプルです。

---

### 差分 7: `go/Makefile` - `make test` の tparse オプション

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Mewst**: `tparse`（合格テストは非表示）
- **Annict**: `tparse -all`（合格テストも表示）
- **Wikino**: `tparse -all`（合格テストも表示）

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: `tparse -all` の方がテスト結果の全体像を把握しやすくなります。`go/CLAUDE.md` にも `tparse -all` の記載があるため統一するのが一貫性があります。

---

### 差分 8: `go/Makefile` - `make test` の bash コマンド表示抑制

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Mewst**: `bash -c`（先頭に `@` なし、コマンド自体が出力される）
- **Annict**: `@bash -c`（先頭に `@` あり、コマンド表示を抑制）
- **Wikino**: `@bash -c`（先頭に `@` あり）

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: `@` を付けることでテスト結果の表示がクリーンになります。

---

### 差分 9: `go/Makefile` - db-migrate コメント文

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Mewst**: 「PostgreSQL 17.6 の pg_dump が生成する」
- **Annict**: 「PostgreSQL 17.6 以降の pg_dump が生成する」
- **Wikino**: 「PostgreSQL 17.6 以降の pg_dump が生成する」

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: 「以降」を付ける方がより正確な記述です。

---

### 差分 10: `go/Makefile` - tparse インストール方法

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Mewst**: `go install github.com/mfridman/tparse`（go.mod のバージョンを使用）
- **Annict**: `go install github.com/mfridman/tparse@latest`（`@latest` を明示指定）
- **Wikino**: `go install github.com/mfridman/tparse`（go.mod のバージョンを使用）

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: go.mod でバージョン管理する方式の方が、チーム全体で同じバージョンを使用でき、再現性が高くなります。

---

### 差分 11: `rails/Makefile` - OP_RUN の APP_ENV 変数

**対象ファイル**: `rails/Makefile`

**差分の内容**:

- **Mewst**: `APP_ENV=$(APP_ENV) op run --env-file=".env.local" --`（APP_ENV 変数付き）
- **Annict**: `op run --env-file=".env.local" --`（APP_ENV 変数なし）
- **Wikino**: `APP_ENV=$(APP_ENV) op run --env-file=".env.local" --`（APP_ENV 変数付き）

**採用方針**:

- [x] Mewst/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: APP_ENV を明示的に渡す方が環境の切り替えが確実です。

---

### 差分 12: `.github/workflows/go-ci.yml` - goimports チェック方法

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Mewst**: `make goimports-check` を独立したステップとして実行
- **Annict**: templ generate ステップ内で `make goimports` → `git diff --exit-code` でチェック
- **Wikino**: Annict と同様のアプローチ

**採用方針**:

- [x] Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Annict/Wikino の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: `make goimports-check` は専用のチェックターゲットであり、独立したステップとして実行する方が CI の失敗原因が明確になります。

---

### 差分 13: `.github/workflows/go-ci.yml` - テスト環境変数 `CI: true`

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Mewst**: `env: CI: true` を明示設定
- **Annict**: `env: CI: true` を明示設定
- **Wikino**: `CI` 環境変数を設定していない

**採用方針**:

- [x] Mewst/Annict の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: GitHub Actions は自動的に `CI=true` を設定しますが、明示的に設定する方が意図が明確です。

---

### 差分 14: `.claude/commands/commit.md` - 分割基準メモの配置

**対象ファイル**: `.claude/commands/commit.md`

**差分の内容**:

- **Mewst/Annict**: 「変更が多岐にわたる場合は機能ごとにコミットを作成してください」がステップ 4 のサブバレットとして記載
- **Wikino**: 同じ内容が「分割の基準」セクション後の独立した段落として記載

**採用方針**:

- [x] Mewst/Annict の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: 構造的な差異のみで内容は同一。ステップ 4 のサブバレットとしてまとまっている方が読みやすいです。

---

### 差分 15: `.claude/commands/review.md` - 第 2 引数の名称（「設計書」vs「作業計画書」）

**対象ファイル**: `.claude/commands/review.md`

**差分の内容**:

- **Mewst**: 「設計書」と記載
- **Annict/Wikino**: 「作業計画書」と記載

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: `docs/plans/` ディレクトリに配置するドキュメントのタイトルは「作業計画書」であるため、「作業計画書」に統一するのが一貫性があります。

---

### 差分 16: `.claude/commands/review.md` - レビューファイルの命名規則

**対象ファイル**: `.claude/commands/review.md`

**差分の内容**:

- **Mewst**: `{ブランチ名}-{YYYYMMDDHHMM}.md` 形式（日時タイムスタンプ）、`docs/reviews/done/` への移動を想定
- **Annict/Wikino**: `{ブランチ名}-{3桁連番}.md` 形式（001, 002...）、`docs/reviews/done/` を検索して最大連番+1 を付与

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: 3 桁連番の方がファイル名が短く、ソート順も自然です。`docs/reviews/done/` も検索対象にする方が連番の一意性が保証されます。

---

### 差分 17: `docs/reviews/template.md` - レビュー後ワークフローと問題点フォーマット

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Mewst**: 旧形式。問題点に「対応方針」「回答」欄がない。末尾に「質問と回答」セクションあり
- **Annict**: 新形式。全問題点に「対応方針」「回答」欄を必須化。ガイドライン参照指示あり。「質問と回答」セクションなし
- **Wikino**: Annict とほぼ同一 + 「設計改善の提案」セクションを追加

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: Wikino の方式が最も発展しており、「対応方針」の必須化と「設計改善の提案」セクションの追加により、レビュープロセスが体系化されています。

---

### 差分 18: `docs/reviews/template.md` - レビュー情報テーブルの「設計書/作業計画書」行

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Mewst**: レビュー情報テーブルに「設計書」行なし（6 項目）
- **Annict**: 「設計書（指定があれば）」行あり（7 項目）
- **Wikino**: 「作業計画書（指定があれば）」行あり（7 項目）

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: レビュー時に参照する作業計画書のパスを記録しておくことで、レビューの文脈が明確になります。差分 15 の採用方針に合わせて「作業計画書」に統一します。

---

### 差分 19: `docs/reviews/template.md` - ファイル命名規則

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Mewst**: `{ブランチ名}.md` 形式。重複時は末尾に連番（例: `sign-up-1-1-1.md`）
- **Annict/Wikino**: `{ブランチ名}-{3桁連番}.md` 形式（例: `sign-up-1-1-001.md`）

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: 差分 16（review.md コマンド側）と合わせて統一します。

---

### 差分 20: `docs/plans/template.md` - 「実装ガイドラインの参照」コメント文言

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Mewst/Wikino**: 「設計を行う前に」「設計の段階で」
- **Annict**: 「作業計画書を作成する前に」「作業計画書作成の段階で」

**採用方針**:

- [x] Annict の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: テンプレート名が「作業計画書」であるため、コメント内の文言も「作業計画書」に統一するのが一貫性があります。

---

### 差分 21: `docs/plans/template.md` - Rails 版ガイドラインの参照リスト

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Mewst/Wikino**: `@rails/CLAUDE.md` のみ（1 項目）
- **Annict**: `@rails/CLAUDE.md` に加えて `@rails/docs/architecture-guide.md`、`@rails/docs/testing-guide.md`、`@rails/docs/security-guide.md` の計 4 項目

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] その他: <!-- 記入 -->

> **補足**: Annict には Rails 版の追加ガイドラインドキュメントが存在しますが、Mewst/Wikino には存在しないため、プロジェクト固有の差分です。

---

### 差分 22: `docs/plans/template.md` - Go 版ガイドラインのリスト順序

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Mewst/Annict**: CLAUDE.md → architecture-guide → handler-guide → i18n-guide → security-guide → templ-guide → validation-guide
- **Wikino**: CLAUDE.md → handler-guide → architecture-guide → validation-guide → i18n-guide → security-guide → templ-guide

**採用方針**:

- [x] Mewst/Annict の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

> **補足**: アルファベット順に近い Mewst/Annict の方が統一的です。

---

## タスクリスト

### フェーズ 1: 採用方針の決定

- [x] **1-1**: 差分レポートを確認し、各差分の採用方針を記入する
  - 各差分の「採用方針」チェックボックスにチェックを入れる
  - 必要に応じて「その他」に補足を記入する
  - **担当**: ユーザー

### フェーズ 2: タスクリストの作成

- [x] **2-1**: 採用方針を元に自プロジェクトのタスクリストを追加する
  - ユーザーが記入した採用方針に基づき、フェーズ 3 以降に自プロジェクトの具体的なタスクを追加する
  - **重要**: 「スキップ（対応不要）」が選ばれた差分は `.claude/commands/sync.md` の「スキップ済みの差分」セクションに追記するタスクも含めること
  - 他プロジェクトへの反映は、各プロジェクトで `/sync` を実行して対応する
  - **担当**: Claude

### フェーズ 3: ファイルの修正

- [x] **3-1**: [Go] Go 関連ファイルの同期（差分 3, 6, 7, 8, 9）

  - `go/CLAUDE.md`: テストヘルパー一覧から `SetupTestDB(t)` を削除（差分 3）
  - `go/.golangci.yml`: templates 層の files パターンを `"**/internal/templates/**"` に変更（差分 6）
  - `go/Makefile`: `make test` の tparse を `tparse -all` に変更（差分 7）
  - `go/Makefile`: `bash -c` → `@bash -c` に変更（差分 8）
  - `go/Makefile`: db-migrate/db-dump コメントを「PostgreSQL 17.6 以降の」に変更（差分 9）
  - **想定ファイル数**: 3 ファイル（実装 3 + テスト 0）
  - **想定行数**: 約 10 行（実装 10 行 + テスト 0 行）

- [x] **3-2**: CLAUDE.md の Dev Container 記載削除と plans/template の修正（差分 1, 2, 20）

  - `CLAUDE.md`: 前提条件から「Dev Container を使用した開発環境」を削除（差分 2）
  - `go/CLAUDE.md`: 「Dev Container を使って開発します」を Docker Compose を使う旨に修正（差分 2）
  - `docs/plans/template.md`: コメント文言を「設計を行う前に」→「作業計画書を作成する前に」に変更（差分 20）
  - `docs/plans/template.md`: handler-guide の説明を「8 種類のみ」→「9 種類のみ」に修正（差分 1）
  - **想定ファイル数**: 3 ファイル（実装 3 + テスト 0）
  - **想定行数**: 約 8 行（実装 8 行 + テスト 0 行）

- [x] **3-3**: レビュー関連ファイルの更新（差分 15, 16, 17, 18, 19）

  - `.claude/commands/review.md`: 「設計書」→「作業計画書」に統一（差分 15）
  - `.claude/commands/review.md`: ファイル命名を 3 桁連番方式に変更（差分 16）
  - `docs/reviews/template.md`: Wikino 形式に更新 - 対応方針・回答必須化、設計改善の提案セクション追加（差分 17）
  - `docs/reviews/template.md`: レビュー情報テーブルに「作業計画書」行追加（差分 18）
  - `docs/reviews/template.md`: ファイル命名規則を 3 桁連番に変更（差分 19）
  - **想定ファイル数**: 2 ファイル（実装 2 + テスト 0）
  - **想定行数**: 約 150 行（実装 150 行 + テスト 0 行）

### フェーズ 4: スキップ済み差分の記録

- [x] **4-1**: `.claude/commands/sync.md` のスキップ済みリストを更新（差分 4, 5, 21）

  - 差分 4: `go/CLAUDE.md`: Go 版で処理するパスの例の詳細度の違い（プロジェクト固有のため）
  - 差分 5: `go/docs/architecture-guide.md`: Presentation 層ヘルパーの image パッケージの有無（プロジェクト固有のため）
  - 差分 21: `docs/plans/template.md`: Rails 版ガイドラインの参照リスト数の違い（プロジェクト固有のため）
  - **想定ファイル数**: 1 ファイル（実装 1 + テスト 0）
  - **想定行数**: 約 5 行（実装 5 行 + テスト 0 行）

### 対応不要の差分（他プロジェクトで対応）

以下の差分は Mewst が採用元のため、Mewst 側での変更は不要です。他プロジェクトで `/sync` を実行した際に反映されます：

- 差分 1: `CLAUDE.md` ハンドラー標準ファイル名の数（Mewst の「9」を採用）→ Annict/Wikino で修正
- 差分 10: `go/Makefile` tparse インストール方法（Mewst/Wikino の方式を採用）→ Annict で修正
- 差分 11: `rails/Makefile` OP_RUN の APP_ENV（Mewst/Wikino の方式を採用）→ Annict で修正
- 差分 12: `go-ci.yml` goimports チェック方法（Mewst の方式を採用）→ Annict/Wikino で修正
- 差分 13: `go-ci.yml` テスト環境変数 CI: true（Mewst/Annict の方式を採用）→ Wikino で修正
- 差分 14: `commit.md` 分割基準メモの配置（Mewst/Annict の方式を採用）→ Wikino で修正
- 差分 22: `docs/plans/template.md` Go 版ガイドラインのリスト順序（Mewst/Annict の方式を採用）→ Wikino で修正
