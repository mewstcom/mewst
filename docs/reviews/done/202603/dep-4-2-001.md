# コードレビュー: dep-4-2

## レビュー情報

| 項目                       | 内容                                     |
| -------------------------- | ---------------------------------------- |
| レビュー日                 | 2026-03-31                               |
| 対象ブランチ               | dep-4-2                                  |
| ベースブランチ             | dep-4-1a                                 |
| 作業計画書（指定があれば） | docs/plans/1_doing/depguard-alignment.md |
| 変更ファイル数             | 13 ファイル                              |
| 変更行数（実装）           | +23 / -199 行                            |
| 変更行数（テスト）         | +10 / -246 行                            |

## 参照するガイドライン

- [@CLAUDE.md#レビュー時に参照するガイドライン](/workspace/CLAUDE.md) - ガイドライン一覧
- [@go/CLAUDE.md](/workspace/go/CLAUDE.md) - Go版の開発ガイド
- [@go/docs/architecture-guide.md](/workspace/go/docs/architecture-guide.md) - アーキテクチャガイド

## 変更ファイル一覧

### 実装ファイル

- [x] `go/cmd/server/main.go`
- [x] `go/internal/dispatcher/dispatcher.go`
- [x] `go/internal/dispatcher/send_email.go`（削除）
- [x] `go/internal/email/sender.go`
- [x] `go/internal/worker/client.go`
- [x] `go/internal/worker/send_email.go`（削除）
- [x] `go/internal/worker/send_email_confirmation.go`

### テストファイル

- [x] `go/internal/dispatcher/dispatcher_test.go`
- [x] `go/internal/email/sender_test.go`
- [x] `go/internal/worker/send_email_confirmation_test.go`
- [x] `go/internal/worker/send_email_test.go`（削除）

### 設定・その他

- [x] `go/.golangci.yml`
- [x] `docs/plans/1_doing/depguard-alignment.md`

## ファイルごとのレビュー結果

問題のあるファイルはありません。全ファイルのチェックボックスにチェック済みです。

### レビュー詳細

各ファイルについて確認した内容：

**`go/.golangci.yml`**: worker-layer に `templates` deny が正しく追加されている。コメントも「フェーズ4で解消予定」から実態に合った説明に更新されている。作業計画書のタスク4-2の仕様通り。

**`go/cmd/server/main.go`**: DI 構成が正しく更新されている。`ConfirmationSender` → `SendEmailConfirmationUsecase` → `Dependencies` の構築順序が適切。作業計画書の「main.go の DI 構成を更新」の要件を満たしている。

**`go/internal/dispatcher/dispatcher.go`**: `EnqueueEmail` メソッドが削除されている。作業計画書の「dispatcher.EnqueueEmail を削除」の要件通り。

**`go/internal/dispatcher/send_email.go`**（削除）: `SendEmailArgs` が削除されている。作業計画書の「dispatcher.SendEmailArgs を削除」の要件通り。

**`go/internal/email/sender.go`**: `SendRaw` / `SendRawInput` がインターフェースと両実装（`ResendSender`, `NoopSender`）から正しく削除されている。`NoopSender` の `SentRawEmails` フィールドも削除済み。`ResendSender.Send` メソッドは `SendRaw` を呼ぶ間接パターンから直接送信するように正しくリファクタリングされている。

**`go/internal/worker/client.go`**: `Dependencies` が `EmailSender email.Sender` から `SendEmailConfirmationUC *usecase.SendEmailConfirmationUsecase` に変更されている。`SendEmailWorker` の登録も削除されている。import も `email` → `usecase` に正しく更新されている。

**`go/internal/worker/send_email.go`**（削除）: `SendEmailWorker` が削除されている。作業計画書の「未使用の SendEmailWorker を削除」の要件通り。

**`go/internal/worker/send_email_confirmation.go`**: Worker が薄い Adapter に変更されている。テンプレートレンダリング、メール件名決定、SendRaw 呼び出しがすべて削除され、UseCase の `Execute` を呼ぶだけのシンプルな実装になっている。アーキテクチャガイドの「Worker は UseCase を呼ぶだけの薄い Adapter にする」方針に一致。ログ出力は `slog.InfoContext` を使用しており、ガイドラインに準拠。

**`go/internal/worker/send_email_confirmation_test.go`**: テストが `NoopSender` → `ConfirmationSender` → `UseCase` → `Worker` の正しい DI チェーンに更新されている。`SentRawEmails` の検証が `SentEmails` に正しく変更されている。テーブル駆動テストのパターンを維持しており、ガイドラインに準拠。

**`go/internal/dispatcher/dispatcher_test.go`**: `EnqueueEmail` 関連のテストと `SendEmailArgs` のテストが正しく削除されている。

**`go/internal/email/sender_test.go`**: `NoopSender_SendRaw` 関連テストが正しく削除されている。

**`go/internal/worker/send_email_test.go`**（削除）: `SendEmailWorker` のテストファイルが削除されている。

**`docs/plans/1_doing/depguard-alignment.md`**: タスク 4-2 のチェックボックスが `[x]` に更新されている。

## 設計との整合性チェック

作業計画書のタスク 4-2 に記載された要件との整合性を確認：

| 要件                                                                                 | 状態                |
| ------------------------------------------------------------------------------------ | ------------------- |
| `send_email_confirmation.go` Worker からテンプレートレンダリングを削除               | ✅                  |
| Worker は UseCase を呼ぶだけに変更                                                   | ✅                  |
| `Sender` インターフェースから `SendRaw` / `SendRawInput` を削除（`Send` のみに統一） | ✅                  |
| 未使用の `SendEmailWorker` を削除                                                    | ✅                  |
| 未使用の `dispatcher.SendEmailArgs` を削除                                           | ✅                  |
| 未使用の `dispatcher.EnqueueEmail` を削除                                            | ✅                  |
| `NoopSender` から `SentRawEmails` フィールドを削除                                   | ✅                  |
| `main.go` の DI 構成を更新                                                           | ✅                  |
| worker-layer に `templates` deny を追加                                              | ✅                  |
| `make lint` で違反がないことを確認                                                   | 未確認（CI で検証） |

すべての要件が正しく実装されています。

## 設計改善の提案

設計改善の提案はありません。

## 総合評価

**評価**: Approve

**総評**:

作業計画書タスク 4-2 の要件がすべて正しく実装されています。

**良かった点**:

- Worker が薄い Adapter に変更され、テンプレートレンダリングやメール件名決定などのロジックが適切に UseCase / email パッケージに委譲されている
- `SendRaw` / `SendRawInput` / `SendEmailWorker` / `SendEmailArgs` / `EnqueueEmail` など未使用コードがきれいに削除されている（YAGNI 原則に合致）
- depguard の worker-layer に `templates` deny が追加され、今後 Worker から templates を直接 import する違反を防止できる
- テストが新しい DI チェーンに正しく更新されている
- 削除行数が追加行数を大きく上回っており（+37 / -448）、コードベースが適切にスリム化されている
