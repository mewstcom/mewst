# プロジェクト間ファイル同期 2026/02/25

## 仕様書

- なし（同期タスクのため仕様書は不要）

## 概要

Mewst・Annict・Wikino の 3 プロジェクト間で共通ファイルの差分を調査し、同期する。

## 設計

### 差分 1: architecture-guide.md に「Repository の WithTx パターン」セクションがない

**対象ファイル**: `go/docs/architecture-guide.md`

**差分の内容**:

- **Mewst**: WithTx パターンのセクションなし
- **Annict**: WithTx パターンのセクションあり（`WithTx` メソッドの目的、メリット、実装例、重要なポイント）
- **Wikino**: WithTx パターンのセクションあり（Annict より詳細、`CreateAccountUsecase` の完全な実装例を含む）

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 2: go/CLAUDE.md に「新規テスト作成時のガイドライン」がない

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Mewst**: `SetupTx` vs `GetTestDB` の使い分け表なし
- **Annict**: テスト戦略セクションに `SetupTx` と `GetTestDB` の使い分け表あり
- **Wikino**: 同上

**採用方針**:

- [x] Annict/Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 3: templ-guide.md の Flash コンポーネント呼び出しで ctx 引数の有無が異なる

**対象ファイル**: `go/docs/templ-guide.md`

**差分の内容**:

- **Mewst**: `@components.Flash(ctx, flash)`（ctx 引数あり）
- **Annict**: `@components.Flash(flash)`（ctx 引数なし）
- **Wikino**: `@components.Flash(flash)`（ctx 引数なし）

templ は ctx を暗黙的に提供するため、ctx を明示的に渡す必要はない（go/CLAUDE.md のテンプレート引数パターンでも ctx を渡さない方針）。

**採用方針**:

- [x] Annict/Wikino の方式を採用（ctx 引数なし）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 4: templ-guide.md / validation-guide.md のテストコメント文言が異なる

**対象ファイル**: `go/docs/templ-guide.md`, `go/docs/validation-guide.md`

**差分の内容**:

- **Mewst**: 「テストDBとトランザクションをセットアップ」
- **Annict**: 「テストDBとトランザクションをセットアップ」（Mewst と同じ）
- **Wikino**: 「共有DB接続プールからトランザクションをセットアップ」

TestMain パターンで DB 接続プールを共有する設計のため、Wikino の表現の方がより正確。

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 5: Dockerfile.dev の golangci-lint バージョンが異なる

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Mewst**: golangci-lint v2.6.2
- **Annict**: golangci-lint v2.7.2
- **Wikino**: golangci-lint v2.7.2

**採用方針**:

- [x] Annict/Wikino の方式を採用（v2.7.2 に更新）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 6: Dockerfile.dev に .zshrc 作成の有無

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Mewst**: `.zshrc` の作成なし
- **Annict**: `RUN touch /home/${USERNAME}/.zshrc` あり
- **Wikino**: `.zshrc` の作成なし

**採用方針**:

- [x] Annict の方式を採用（.zshrc 作成を追加）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 7: Dockerfile.dev に mise の Docker 環境向け推奨設定コメントの有無

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Mewst**: mise セクションに Docker 環境向けコメントなし
- **Annict**: 同上
- **Wikino**: `# Docker環境向けの推奨設定: shims方式を使用` コメントあり

**採用方針**:

- [x] Wikino の方式を採用（コメント追加）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 8: go/Makefile の tparse に -all オプションがない

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Mewst**: `test-pkg` と `test-run` で `tparse`（オプションなし）
- **Annict**: `tparse -all`（合格テストも含めて表示）
- **Wikino**: `tparse -all`（同上）

`-all` オプションにより、失敗テストだけでなく合格テストも表示されるため、テスト結果の確認がしやすい。

**採用方針**:

- [x] Annict/Wikino の方式を採用（`-all` オプション追加）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 9: go-ci.yml の golangci-lint バージョンが異なる

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Mewst**: golangci-lint v2.6.2
- **Annict**: golangci-lint v2.7.2
- **Wikino**: golangci-lint v2.6.2

差分 5 の Dockerfile.dev と合わせて更新すべき。

**採用方針**:

- [x] Annict の方式を採用（v2.7.2 に更新）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 10: rails/.prettierrc が Mewst に存在しない

**対象ファイル**: `rails/.prettierrc`

**差分の内容**:

- **Mewst**: ファイルが存在しない
- **Annict**: `.prettierrc` あり
- **Wikino**: `.prettierrc` あり（Annict と同一内容）

Mewst では Oxfmt をプロジェクトルートで使用しているが、rails-ci.yml に prettier ジョブがないため .prettierrc は不要かもしれない。ただし、Annict/Wikino では rails-ci.yml に prettier ジョブがある。

**採用方針**:

- [ ] Annict/Wikino の方式を採用（.prettierrc を追加）
- [x] スキップ（対応不要）(Recommended)
- [ ] その他: <!-- 記入 -->

---

### 差分 11: docs/plans/template.md のフェーズ 2/3 サンプルにプラットフォームプレフィックスがない

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Mewst**: フェーズ 2/3 のサンプルタスクに `[Go]`/`[Rails]` プレフィックスがない（フェーズ 1 にはある）
- **Annict**: すべてのフェーズに `[Go]`/`[Rails]` プレフィックスあり
- **Wikino**: 同上

テンプレートの一貫性の問題。

**採用方針**:

- [x] Annict/Wikino の方式を採用（プレフィックス追加）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 12: docs/reviews/template.md のガイドライン参照先が不正確

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Mewst**: `@CLAUDE.md#ログ出力` を参照（プロジェクトルートの CLAUDE.md）
- **Annict**: `@go/CLAUDE.md#ログ出力` を参照（Go 版の CLAUDE.md）
- **Wikino**: `@CLAUDE.md#ログ出力` を参照（Mewst と同じ）

ログ出力のガイドラインは `go/CLAUDE.md` に記載されているため、Annict の参照先の方が正確。

**採用方針**:

- [x] Annict の方式を採用（`@go/CLAUDE.md` に修正）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 13: docs/reviews/template.md に「対応方針」「回答」必須の補足がない

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Mewst**: `### path/to/file1.go（例: 修正方針が明確な場合）`
- **Annict**: `### path/to/file1.go（例: 修正方針が明確な場合でも「対応方針」「回答」は必須）`
- **Wikino**: Mewst と同じ

Annict の補足により、修正方針が明確な場合でも「対応方針」と「回答」を省略しないことが明確になる。

**採用方針**:

- [x] Annict の方式を採用（補足を追加）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

## タスクリスト

### フェーズ 1: 採用方針の決定

- [x] **1-1**: 差分レポートを確認し、各差分の採用方針を記入する
  - 各差分の「採用方針」チェックボックスにチェックを入れる
  - 必要に応じて「その他」に補足を記入する
  - **担当**: ユーザー

### フェーズ 2: タスクリストの作成

- [x] **2-1**: 採用方針を元に自プロジェクトのタスクリストを追加する
  - ユーザーが記入した採用方針に基づき、フェーズ 3 以降に自プロジェクトの具体的なタスクを追加する
  - **重要**: 「スキップ（対応不要）」が選ばれた差分は `.claude/commands/sync.md` の「スキップ済みの差分」セクションに追記するタスクも含めること
  - 他プロジェクトへの反映は、各プロジェクトで `/sync` を実行して対応する
  - **担当**: Claude

### フェーズ 3: ガイドライン・ドキュメントの同期

- [x] **3-1**: `go/docs/architecture-guide.md` に「Repository の WithTx パターン」セクションを追加（差分 1）
  - Wikino の方式を採用し、以下の内容を含むセクションを追加する:
    - `WithTx` メソッドの目的（トランザクション内でリポジトリを使用）
    - メリット（トランザクション境界の明確化、ロールバック安全性）
    - `CreateAccountUsecase` の完全な実装例
    - 重要なポイント（defer Rollback、WithTx の呼び出しタイミング）
  - **対象ファイル**: `go/docs/architecture-guide.md`
  - **担当**: Claude

- [x] **3-2**: `go/CLAUDE.md` に `SetupTx` / `GetTestDB` の使い分け表を追加（差分 2）
  - テスト戦略セクションの「新規テストパッケージの作成手順」付近に使い分け表を追加する:
    - `SetupTx(t)`: Repository テスト等、トランザクション内で完結するテスト
    - `GetTestDB()`: Usecase テスト等、自前でトランザクション管理を行うテスト
  - **対象ファイル**: `go/CLAUDE.md`
  - **担当**: Claude

- [x] **3-3**: `go/docs/templ-guide.md` の Flash コンポーネント呼び出しから ctx 引数を削除（差分 3）
  - `@components.Flash(ctx, flash)` → `@components.Flash(flash)` に修正
  - templ は ctx を暗黙的に提供するため、明示的に渡す必要はない
  - **対象ファイル**: `go/docs/templ-guide.md`（507 行目付近）
  - **担当**: Claude

- [x] **3-4**: `go/docs/templ-guide.md` / `go/docs/validation-guide.md` のテストコメント文言を修正（差分 4）
  - 「テストDBとトランザクションをセットアップ」→「共有DB接続プールからトランザクションをセットアップ」に修正
  - TestMain パターンで DB 接続プールを共有する設計のため、Wikino の表現の方がより正確
  - **対象ファイル**: `go/docs/templ-guide.md`（370 行目）、`go/docs/validation-guide.md`（452 行目）
  - **担当**: Claude

### フェーズ 4: 開発環境・CI 設定の同期

- [x] **4-1**: `Dockerfile.dev` の golangci-lint を v2.7.2 に更新（差分 5）
  - `v2.6.2` → `v2.7.2` に更新
  - **対象ファイル**: `Dockerfile.dev`（80 行目）
  - **担当**: Claude

- [x] **4-2**: `Dockerfile.dev` に `.zshrc` 作成を追加（差分 6）
  - ユーザー作成後に `RUN touch /home/${USERNAME}/.zshrc` を追加
  - **対象ファイル**: `Dockerfile.dev`（85 行目付近）
  - **担当**: Claude

- [x] **4-3**: `Dockerfile.dev` に mise の Docker 環境向け推奨設定コメントを追加（差分 7）
  - mise セクション（51 行目付近）に `# Docker環境向けの推奨設定: shims方式を使用` コメントを追加
  - **対象ファイル**: `Dockerfile.dev`
  - **担当**: Claude

- [x] **4-4**: `go/Makefile` の `test-pkg` / `test-run` で `tparse` に `-all` オプションを追加（差分 8）
  - `tparse` → `tparse -all` に修正（合格テストも含めて表示）
  - **対象ファイル**: `go/Makefile`（50 行目、66 行目）
  - **担当**: Claude

- [x] **4-5**: `.github/workflows/go-ci.yml` の golangci-lint を v2.7.2 に更新（差分 9）
  - `v2.6.2` → `v2.7.2` に更新
  - **対象ファイル**: `.github/workflows/go-ci.yml`（44 行目）
  - **担当**: Claude

### フェーズ 5: ドキュメントテンプレートの同期

- [x] **5-1**: `docs/plans/template.md` のフェーズ 2/3 にプラットフォームプレフィックスを追加（差分 11）
  - フェーズ 2/3 のサンプルタスクに `[Go]`/`[Rails]` プレフィックスを追加
  - フェーズ 1 と同じ形式に統一する
  - **対象ファイル**: `docs/plans/template.md`（196-214 行目付近）
  - **担当**: Claude

- [x] **5-2**: `docs/reviews/template.md` のガイドライン参照先を `@go/CLAUDE.md` に修正（差分 12）
  - `@CLAUDE.md#ログ出力` → `@go/CLAUDE.md#ログ出力` に修正
  - ログ出力のガイドラインは `go/CLAUDE.md` に記載されているため
  - **対象ファイル**: `docs/reviews/template.md`（108 行目）
  - **担当**: Claude

- [x] **5-3**: `docs/reviews/template.md` に「対応方針」「回答」必須の補足を追加（差分 13）
  - `（例: 修正方針が明確な場合）` → `（例: 修正方針が明確な場合でも「対応方針」「回答」は必須）` に修正
  - **対象ファイル**: `docs/reviews/template.md`（98 行目）
  - **担当**: Claude

### フェーズ 6: スキップ済み差分の更新

- [x] **6-1**: `.claude/commands/sync.md` にスキップ済み差分を追記（差分 10）
  - 差分 10（`rails/.prettierrc` が Mewst に存在しない）をスキップ済みの差分に追記
  - **対象ファイル**: `.claude/commands/sync.md`
  - **担当**: Claude
