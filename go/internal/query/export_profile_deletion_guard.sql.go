// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: export_profile_deletion_guard.sql

package query

import (
	"context"
	"database/sql"

	"github.com/google/uuid"
)

const acquireExportProfileDeletionLock = `-- name: AcquireExportProfileDeletionLock :exec
SELECT pg_advisory_lock($1::bigint)
`

func (q *Queries) AcquireExportProfileDeletionLock(ctx context.Context, dollar_1 int64) error {
	_, err := q.db.ExecContext(ctx, acquireExportProfileDeletionLock, dollar_1)
	return err
}

const acquireExportProfileOperationLock = `-- name: AcquireExportProfileOperationLock :exec
SELECT pg_advisory_lock_shared($1::bigint)
`

func (q *Queries) AcquireExportProfileOperationLock(ctx context.Context, dollar_1 int64) error {
	_, err := q.db.ExecContext(ctx, acquireExportProfileOperationLock, dollar_1)
	return err
}

const getExportProfileDeletionStartedAt = `-- name: GetExportProfileDeletionStartedAt :one
SELECT export_deletion_started_at
FROM profiles
WHERE id = $1
`

// Read the persistent marker. Export operations call this before waiting for
// the shared advisory lock and again while holding it, so post-deletion work
// stops promptly without reopening the check-to-lock race. A missing profile
// has no export work either and is represented by sql.ErrNoRows.
//
// [Ja] 永続マーカーを読む。export 操作は共有 advisory lock を待つ前と取得後に
// 呼び出すため、削除開始後の処理を速やかに止めつつ、確認と lock の間の競合も
// 開け直さない。存在しないプロフィールにも export 作業は無く、sql.ErrNoRows として返す。
func (q *Queries) GetExportProfileDeletionStartedAt(ctx context.Context, id uuid.UUID) (sql.NullTime, error) {
	row := q.db.QueryRowContext(ctx, getExportProfileDeletionStartedAt, id)
	var export_deletion_started_at sql.NullTime
	err := row.Scan(&export_deletion_started_at)
	return export_deletion_started_at, err
}

const markExportProfileDeletionStarted = `-- name: MarkExportProfileDeletionStarted :one
UPDATE profiles
SET export_deletion_started_at = COALESCE(export_deletion_started_at, NOW())
WHERE id = $1
RETURNING export_deletion_started_at
`

// Persist the deletion marker before waiting for in-flight export operations. The
// existing value is kept on retries, while the UPDATE still takes the profile
// row lock that serializes it with CreateExport's FOR SHARE gate.
//
// [Ja] 進行中の export 操作を待つ前に削除マーカーを永続化する。再実行では既存値を
// 維持しつつ、CreateExport の FOR SHARE ガードと直列化するプロフィール行ロックは
// UPDATE によって取得する。
func (q *Queries) MarkExportProfileDeletionStarted(ctx context.Context, id uuid.UUID) (sql.NullTime, error) {
	row := q.db.QueryRowContext(ctx, markExportProfileDeletionStarted, id)
	var export_deletion_started_at sql.NullTime
	err := row.Scan(&export_deletion_started_at)
	return export_deletion_started_at, err
}

const releaseExportProfileDeletionLock = `-- name: ReleaseExportProfileDeletionLock :one
SELECT pg_advisory_unlock($1::bigint)
`

func (q *Queries) ReleaseExportProfileDeletionLock(ctx context.Context, dollar_1 int64) (bool, error) {
	row := q.db.QueryRowContext(ctx, releaseExportProfileDeletionLock, dollar_1)
	var pg_advisory_unlock bool
	err := row.Scan(&pg_advisory_unlock)
	return pg_advisory_unlock, err
}

const releaseExportProfileOperationLock = `-- name: ReleaseExportProfileOperationLock :one
SELECT pg_advisory_unlock_shared($1::bigint)
`

func (q *Queries) ReleaseExportProfileOperationLock(ctx context.Context, dollar_1 int64) (bool, error) {
	row := q.db.QueryRowContext(ctx, releaseExportProfileOperationLock, dollar_1)
	var pg_advisory_unlock_shared bool
	err := row.Scan(&pg_advisory_unlock_shared)
	return pg_advisory_unlock_shared, err
}
